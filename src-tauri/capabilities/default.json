{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "外壳只需要一件事：把后端 sidecar 拉起来。别的什么都不给。",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "binaries/dreamtexture", "sidecar": true }]
    }
  ]
}
